export default function signupMail(fullname:string, mail: string, otp:number){
    return `We are very happy to have in our family Mr/Miss ${fullname}
    Kinldy verify your email ${mail} and otp ${otp} 
    `
}